# 班级工作台 实现计划

> **For agentic workers:** 用 superpowers:executing-plans 或 subagent-driven-development 逐任务实现。步骤用 `- [ ]` 跟踪。
> 设计基准见 [`2026-06-14-班级工作台-design.md`](2026-06-14-班级工作台-design.md)。

**Goal:** 把「考点画像」(error-profile) 与「学生管理」(student-timeline) 合并成一页「班级工作台」——顶部统一选班+班级管理，下面分段切换「考点视角／学生视角」，班级人数口径统一为「名单∪成绩」根除不同步。

**Architecture:** 纯前端 vanilla JS。复用 `student-timeline` 页容器作宿主；控制器把原两个 render 拆成「拉数据+顶部+分段」的 `renderWorkbenchPage` + 两个无 fetch/无班级 chips 的 body 渲染器；班级列表构建抽成纯函数单测兜住同步修复。`error-profile` dock 入口移除并重定向到宿主。

**Tech Stack:** 现有 `GrammarErrorProfile*` 模块、`GrammarStudentTracking`、Supabase 云端 `exam_results`、node:test 单测、Playwright smoke。

---

## 文件映射

- `docs/grammar-fill/modules/error-profile-store.js` — 新增纯函数 `buildClassList(allRows, localClasses)`（名单∪成绩，口径唯一）。
- `test/error-profile-store.test.js` — `buildClassList` 单测（含 0→40 修复用例）。
- `docs/grammar-fill/modules/error-profile-render.js` — 新增 `workbenchTabsHtml(activeTab)`（分段切换）。
- `docs/grammar-fill/modules/error-profile-controller.js` — 拆 `renderBoardBody`/`renderTimelineBody`（无 fetch、无班级 chips），新增 `renderWorkbenchPage`（顶部统一+分段+委派）；导出收敛为 `renderImportPage` + `renderWorkbenchPage`（保留 `renderBoardPage`/`renderTimelinePage` 作兼容别名指向 `renderWorkbenchPage`）。
- `docs/grammar-fill/app.js` — 新增 `renderWorkbenchPage()`（合并两套 deps）；`switchPage` 把 `error-profile` 重定向到 `student-timeline`；分发处 `student-timeline → renderWorkbenchPage`。
- `docs/grammar-fill/index.html` — `page-student-timeline` 标题改「班级工作台」；移除「考点画像」dock 项；移除 `page-error-profile` 容器。
- `docs/grammar-fill/modules/app-state.js` — `normalizeDockKey`/`getDockKeyForPage` 把 `error-profile` 归到 `student-timeline`；`error-profile` 仍保留在 `isProtectedPage`（重定向前的鉴权安全）。
- `scripts/check_grammar_modules.py` — 登记新导出 `buildClassList`、`workbenchTabsHtml`。
- `tests/smoke.spec.js` — 合并更新「考点画像／学生管理／班级管理」用例为「班级工作台」用例（含同步一致断言、tab 切换、老入口重定向）。
- `PROJECT_LOG.md` — 追加条目。

---

## Task 1: 纯函数 buildClassList（同步修复的核心，先 TDD）

**Files:**
- Modify: `docs/grammar-fill/modules/error-profile-store.js`（导出对象 + 新函数）
- Test: `test/error-profile-store.test.js`

- [ ] **Step 1: 写失败测试**（追加到 `test/error-profile-store.test.js`，并在顶部解构里加入 `buildClassList`）

```js
test('buildClassList: 人数=名单∪成绩（修两页 0 vs 40 口径不同步）', () => {
  const rows = [
    { class_id: 'c1', class_name: '高三①班', student_no: 'S1' },
    { class_id: 'c1', class_name: '高三①班', student_no: 'S2' },
  ];
  const local = [
    { id: 'c1', name: '高三①班', students: ['S2', 'S3'] },   // S2 重叠、S3 仅名单
    { id: 'cDemo', name: '演示班级', students: ['m1', 'm2', 'm3'] }, // 纯名单、无成绩
  ];
  const list = buildClassList(rows, local);
  const byId = {}; list.forEach((c) => { byId[c.id] = c; });
  assert.equal(byId.c1.count, 3);        // S1,S2,S3 并集
  assert.equal(byId.cDemo.count, 3);     // 纯名单也算 3（不再是 0）
  assert.equal(byId.c1.name, '高三①班');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/error-profile-store.test.js`
Expected: FAIL（`buildClassList is not a function`）

- [ ] **Step 3: 实现 `buildClassList`**（加到 `error-profile-store.js`，导出对象里加 `buildClassList: buildClassList`）

```js
// 班级列表（云端成绩 ∪ 本地名单），人数口径唯一：两视角共用，杜绝 0 vs 40。
function buildClassList(allRows, localClasses) {
  var clsMap = {};
  (allRows || []).forEach(function(r){
    if (!r.class_id) return;
    if (!clsMap[r.class_id]) clsMap[r.class_id] = { id: r.class_id, name: r.class_name || r.class_id, stu: {} };
    clsMap[r.class_id].stu[r.student_no] = 1;
  });
  (localClasses || []).forEach(function(c){
    if (!clsMap[c.id]) clsMap[c.id] = { id: c.id, name: c.name, stu: {} };
    (c.students || []).forEach(function(n){ if (n) clsMap[c.id].stu[n] = 1; });
  });
  return Object.keys(clsMap).map(function(k){
    return { id: clsMap[k].id, name: clsMap[k].name, count: Object.keys(clsMap[k].stu).length };
  });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/error-profile-store.test.js`
Expected: PASS（全部）

- [ ] **Step 5: 提交**

```bash
git add docs/grammar-fill/modules/error-profile-store.js test/error-profile-store.test.js
git commit -m "feat(error-profile): buildClassList 统一班级人数口径(名单∪成绩)"
```

---

## Task 2: 分段切换渲染 workbenchTabsHtml

**Files:**
- Modify: `docs/grammar-fill/modules/error-profile-render.js`（新函数 + 导出）

- [ ] **Step 1: 实现 `workbenchTabsHtml`**（加到 render 模块，导出 `workbenchTabsHtml`）

```js
// 工作台分段切换：考点视角 / 学生视角。activeTab ∈ {'board','students'}。
function workbenchTabsHtml(activeTab) {
  function seg(key, label) {
    var on = activeTab === key;
    return '<button type="button" class="wb-tab" data-tab="' + key + '" '
      + 'style="flex:1;padding:9px 0;border:none;cursor:pointer;font-size:14px;border-radius:9px;'
      + (on ? 'background:#fff;color:#0071e3;font-weight:600;box-shadow:0 1px 3px rgba(0,0,0,.08);' : 'background:transparent;color:#666;') + '">'
      + label + '</button>';
  }
  return '<div style="display:flex;gap:4px;background:#f0f0f2;border-radius:11px;padding:4px;margin:14px 0 12px;max-width:420px;">'
    + seg('board', '考点视角') + seg('students', '学生视角') + '</div>';
}
```

- [ ] **Step 2: 校验纯模块契约**

Run: `python3 scripts/check_grammar_modules.py`
Expected: 报错缺 `workbenchTabsHtml` 登记——契约登记在 Task 7 Step 1 统一补；此处可先跳过校验，到 Task 7 一并跑绿。

- [ ] **Step 3: 提交**

```bash
git add docs/grammar-fill/modules/error-profile-render.js
git commit -m "feat(error-profile): workbenchTabsHtml 分段切换渲染"
```

---

## Task 3: 控制器——拆 body 渲染器 + renderWorkbenchPage

**Files:**
- Modify: `docs/grammar-fill/modules/error-profile-controller.js`

模块内状态统一为：`_wb`(deps)、`_wbClassId`、`_wbTab`(默认 `'board'`)、保留 `_brdExams/_boardExamId/_brdTrendByCat`、`_tlClasses`。

- [ ] **Step 1: 抽 `renderBoardBody(containerEl, allRows, classId)`**

从现 `renderBoardPage`（行 174-229）抽出「按 classId 过滤 rows → 组 `_brdExams` → 算 orderedExams/trends → 渲染 examChips + epBoardDetail + catTrendDetails」这段，**去掉**：自带的 `classChipsHtml`、`.ep-class-chip`/`.ep-class-new` wiring、`fetchExamResults`、`epToTimeline` 改为切到学生 tab。容器从 `errorProfileContent` 改为传入的 `containerEl`。`boardViewProfile`/`boardDelProfile` 改用 `_wb`（删卷后调 `renderWorkbenchPage(_wb)`）。「查看单个学生画像」按钮：`_wbTab='students'; renderWorkbenchPage(_wb)`。

- [ ] **Step 2: 抽 `renderTimelineBody(containerEl, allRows, classId)`**

从现 `renderTimelinePage`（行 405-415）抽出「按 classId 过滤 rows → rosterSet(成绩∪classRoster) → buildStudentTimeline → studentSearchBoxHtml + wireStudentSearch」，渲染进 `containerEl`。`tlRemoveStudent`/`tlAddStudents`/`tlRenameClass`/`tlDeleteClass` 成功回调改调 `renderWorkbenchPage(_wb)`。

- [ ] **Step 3: 写 `renderWorkbenchPage(deps)`**

```js
function renderWorkbenchPage(deps) {
  _wb = deps || _wb;
  var el = document.getElementById('studentTimelineContent');
  if (!el) return;
  el.innerHTML = '<div style="color:#888;padding:18px 4px;">加载云端数据…</div>';
  (_wb.fetchExamResults ? _wb.fetchExamResults() : Promise.resolve({ rows: [] })).then(function(res){
    var allRows = (res && res.rows) || [];
    var classList = window.GrammarErrorProfileStore.buildClassList(allRows, (_wb.getClasses && _wb.getClasses()) || []);
    if ((!_wbClassId || !classList.some(function(c){ return c.id === _wbClassId; })) && classList.length) _wbClassId = classList[0].id;
    _tlClasses = classList;
    var R = window.GrammarErrorProfileRender;
    var manageRow = _wbClassId
      ? '<div style="margin:6px 0 4px;display:flex;gap:8px;flex-wrap:wrap;align-items:center;">'
          + '<span id="stRosterDrop" style="display:inline-block;padding:7px 14px;border-radius:999px;border:1px dashed #cfe3ff;background:#f7fbff;color:#0071e3;cursor:pointer;font-size:13px;">＋ 导入名单（学号+姓名 Excel）</span>'
          + '<button type="button" id="stRenameClass" style="padding:7px 14px;border-radius:999px;border:1px solid #cfe3ff;background:#f7fbff;color:#0071e3;cursor:pointer;font-size:13px;">✎ 班级改名</button>'
          + '<button type="button" id="stDeleteClass" style="padding:7px 14px;border-radius:999px;border:1px solid #f3c0c0;background:#fff;color:#c0392b;cursor:pointer;font-size:13px;">🗑 删除该班</button>'
          + '<input type="file" id="stRosterFile" accept=".xls,.xlsx" style="display:none;"></div>'
      : '<div style="margin:6px 0 4px;color:#888;font-size:13px;">先「＋ 新建班级」，再导名单/导成绩或加学生。</div>';
    el.innerHTML = R.classChipsHtml(classList, _wbClassId)
      + manageRow
      + (_wbClassId ? R.studentAddBoxHtml() : '')
      + (_wbClassId ? R.workbenchTabsHtml(_wbTab) : '')
      + '<div id="wbContent"></div>';
    // 顶部统一 wiring：班级 chips / 新建 / 改名 / 删班 / 加学生 / 导名单（沿用原 id 与处理函数）
    el.querySelectorAll('.ep-class-chip').forEach(function(btn){
      btn.addEventListener('click', function(){ _wbClassId = btn.getAttribute('data-id'); _boardExamId = null; renderWorkbenchPage(_wb); });
    });
    var newChip = el.querySelector('.ep-class-new');
    if (newChip) newChip.addEventListener('click', function(){
      var name = window.prompt && window.prompt('新建班级名（如 高三①班）：');
      if (name && _wb.createClass) { var c = _wb.createClass(name); if (c) _wbClassId = c.id; renderWorkbenchPage(_wb); }
    });
    wireManageRow();   // 内含 stRenameClass/stDeleteClass/stAddBtn/stRosterDrop 的既有处理
    var tabsEl = el.querySelectorAll('.wb-tab');
    tabsEl.forEach(function(btn){ btn.addEventListener('click', function(){ _wbTab = btn.getAttribute('data-tab'); renderWorkbenchPage(_wb); }); });
    var body = document.getElementById('wbContent');
    if (!_wbClassId) { if (body) body.innerHTML = '<div style="color:#888;padding:18px 4px;">先建个班，再导成绩/名单或加学生。</div>'; return; }
    if (_wbTab === 'students') renderTimelineBody(body, allRows, _wbClassId);
    else renderBoardBody(body, allRows, _wbClassId);
  }).catch(function(){ el.innerHTML = '<div style="color:#888;padding:18px 4px;">加载失败，稍后重试（请确认已登录）。</div>'; });
}
```

> `wireManageRow()` 把现 `renderTimelinePage` 里 delClassBtn/renameBtn/addBtn/rosterDrop 的 wiring（行 391-402）原样搬来。`tlDeleteClass/tlRenameClass/tlAddStudents/stImportRoster` 内部把 `_tlClassId` 全部换成 `_wbClassId`，成功回调改 `renderWorkbenchPage(_wb)`。

- [ ] **Step 4: 导出**

```js
window.GrammarErrorProfileController = {
  renderImportPage: renderImportPage,
  renderWorkbenchPage: renderWorkbenchPage,
  renderBoardPage: renderWorkbenchPage,      // 兼容别名
  renderTimelinePage: renderWorkbenchPage    // 兼容别名
};
```

- [ ] **Step 5: 提交**

```bash
git add docs/grammar-fill/modules/error-profile-controller.js
git commit -m "refactor(error-profile): 控制器合并为 renderWorkbenchPage(顶部统一+分段委派)"
```

---

## Task 4: app.js —— 合并 deps + 路由重定向

**Files:**
- Modify: `docs/grammar-fill/app.js`

- [ ] **Step 1: 写 `renderWorkbenchPage()`（合并两套 deps）**

把 `renderErrorProfilePage`(1263-1281) 与 `renderStudentTimelinePage`(1282-…) 的 deps 并成一份，调用 `window.GrammarErrorProfileController.renderWorkbenchPage({ ... })`。键全集：`loadProfiles, saveProfiles, catNames, addExamQuestionToErrorBook, getClasses, createClass, deleteClass, deleteExamResultsClass, deleteExamResultsExam, loadStudentNames, fetchExamResults, classRoster, importRoster, renameClass, addStudents, removeStudent`（去掉 `gotoTimeline`——改为内部切 tab）。两个旧函数删除或保留为 `function renderErrorProfilePage(){ return renderWorkbenchPage(); }` 兼容。

- [ ] **Step 2: 路由重定向 + 分发**

在 `switchPage` 入口（app.js）加：`if (page === 'error-profile') page = 'student-timeline';`。分发处把 `1722 renderErrorProfilePage()` 删除，`1724` 改为 `if (page === 'student-timeline') renderWorkbenchPage();`。

- [ ] **Step 3: 手测冒烟（本地预览）**

Run: 起 worktree 预览，seed 一个纯名单班 + 一个有成绩班，`switchPage('student-timeline')`，确认默认考点视角出画像、切学生视角能搜、班级 chip 人数两 tab 一致、`switchPage('error-profile')` 落到工作台。
Expected: 无 console error。

- [ ] **Step 4: 提交**

```bash
git add docs/grammar-fill/app.js
git commit -m "feat(error-profile): app.js 合并工作台 deps + error-profile 重定向到工作台"
```

---

## Task 5: index.html —— 标题/入口收敛

**Files:**
- Modify: `docs/grammar-fill/index.html`

- [ ] **Step 1:** `page-student-timeline` 标题（行 398 `<h1>学生管理</h1>`）改为 `班级工作台`，page-desc 改「选班 → 考点视角看该讲什么 / 学生视角管名单看个人」。
- [ ] **Step 2:** 删除「考点画像」dock 项（行 486 `data-dock-key="error-profile"` 整个 `<div class="dock-item">`）。「学生管理」dock 项（行 490）`data-label` 改「班级工作台」。
- [ ] **Step 3:** 删除 `page-error-profile` 容器（行 385-388 含 `errorProfileContent`）——已无渲染入口。
- [ ] **Step 4: 提交**

```bash
git add docs/grammar-fill/index.html
git commit -m "refactor(error-profile): index 收敛——学生管理→班级工作台、去考点画像入口/容器"
```

---

## Task 6: app-state —— dock key 收敛

**Files:**
- Modify: `docs/grammar-fill/modules/app-state.js`

- [ ] **Step 1:** `getDockKeyForPage`（314-): 因 `switchPage` 已把 error-profile 重定向到 student-timeline，正常不会以 error-profile 进入；为稳妥，在 `normalizeDockKey`/`getDockKeyForPage` 中把传入 `error-profile` 归一为 `student-timeline`（`if (page === 'error-profile') page = 'student-timeline';` 于函数开头）。`isProtectedPage` 保留 error-profile（重定向前鉴权）。`normalizePageKey` 不动（error-profile 仍是合法 key，重定向在 switchPage 完成）。
- [ ] **Step 2: 校验**

Run: `python3 scripts/check_grammar_modules.py`（app-state 在 EXPECTED_MODULES，确认导出未破）
Expected: OK。

- [ ] **Step 3: 提交**

```bash
git add docs/grammar-fill/modules/app-state.js
git commit -m "refactor(app-state): error-profile dock key 归一到 student-timeline(班级工作台)"
```

---

## Task 7: 契约 + smoke 更新

**Files:**
- Modify: `scripts/check_grammar_modules.py`、`tests/smoke.spec.js`

- [ ] **Step 1: 登记新导出**：`error-profile-store.js` 加 `buildClassList`；`error-profile-render.js` 加 `workbenchTabsHtml`。

Run: `python3 scripts/check_grammar_modules.py`
Expected: OK（27 模块）。

- [ ] **Step 2: smoke 改造**：把现「错题画像：导入页可进 + 画像板块列出并展开」「学生时间线…」「班级管理…新建→删除」「班级管理…改名+添加+删除」四条改写为「班级工作台」用例：
  - 进 `switchPage('student-timeline')` → 默认 `.wb-tab[data-tab="board"]` 激活、出 examChips/画像；
  - 点 `.wb-tab[data-tab="students"]` → 出 `#stSearch`、`.st-pick`、`.st-del`；
  - 构造一个纯名单班（localStorage 有 students、云端无 rows）→ 两个 tab 下班级 chip 人数一致（断言 chip 文案含该数）；
  - `switchPage('error-profile')` → `#page-student-timeline` active（重定向）。

- [ ] **Step 3: 跑 smoke**

Run: `npx playwright test tests/smoke.spec.js --project=chromium -g "班级工作台"`
Expected: PASS。

- [ ] **Step 4: 提交**

```bash
git add scripts/check_grammar_modules.py tests/smoke.spec.js
git commit -m "test(error-profile): 班级工作台 smoke + 契约登记 buildClassList/workbenchTabsHtml"
```

---

## Task 8: 全量门禁 + 文档

- [ ] **Step 1: 全量门禁**

Run: `npm run check`
Expected: `OK: all engineering checks passed`（smoke 全绿）。

- [ ] **Step 2: PROJECT_LOG 追加条目**（合并动机、同步修复、导航收敛、验证）。

- [ ] **Step 3: 提交**

```bash
git add PROJECT_LOG.md
git commit -m "docs: PROJECT_LOG 记录班级工作台合并"
```

---

## 自检对照（spec → task）

- 合并成一页/分段 → Task 3/4/5。
- 人数口径统一(名单∪成绩) → Task 1（纯函数+单测）+ Task 3（工作台用它）。
- dock 收一个 + error-profile 重定向 → Task 4(switchPage) + 5(index) + 6(app-state)。
- 导入页独立、不动 → 不在范围（仅 Task 5 顺手不改 error-import）。
- 隐私红线不变 → 不动数据模型/云端。
- 测试 → Task 1(单测) + Task 7(smoke) + Task 8(全量)。
